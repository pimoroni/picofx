name: MicroPython

on:
  push:
  pull_request:
  release:
    types: [created]

jobs:
  build:
    name: MicroPython ${{ matrix.shortname }}
    runs-on: ubuntu-24.04
    continue-on-error: true
    strategy:
      matrix:
        include:
          - name: TinyFX
            shortname: tiny_fx
            board: PIMORONI_TINYFX
          - name: TinyFX W
            shortname: tiny_fx_w
            board: PIMORONI_TINYFX
            variant: w

    env:
      # MicroPython version will be contained in github.event.release.tag_name for releases
      CI_RELEASE_FILENAME: ${{ matrix.shortname }}-${{ github.event.release.tag_name || github.sha }}-micropython
      CI_PROJECT_ROOT: ${{ github.workspace }}/src-${{ github.sha }}
      CI_BUILD_ROOT: ${{ github.workspace }}
      CI_USE_ENV: 1

    steps:
    - name: Compiler Cache Fixup
      run: |
        mkdir -p /home/runner/.ccache
  
    - name: "CCache: Restore saved cache"
      uses: actions/cache@v5
      with:
        path: /home/runner/.ccache
        key: ccache-micropython-${{ matrix.shortname }}-${{ github.ref }}-${{ github.sha }}
        restore-keys: |
          ccache-micropython-${{ matrix.shortname }}-${{ github.ref }}
          ccache-micropython-${{ matrix.shortname }}-

    - name: "Checkout Project"
      uses: actions/checkout@v6
      with:
        submodules: true
        path: ${{ env.CI_PROJECT_ROOT }}

    - name: "Install Arm GNU Toolchain (arm-none-eabi-gcc)"
      uses: carlosperate/arm-none-eabi-gcc-action@v1
      with:
        release: '14.2.Rel1'

    - name: "Prepare tools & dependencies"
      shell: bash
      run: |
        source $CI_PROJECT_ROOT/ci/micropython.sh
        mkdir -p $CI_BUILD_ROOT
        ci_apt_install_build_deps
        ci_prepare_all

    - name: "MicroPython: Configure"
      shell: bash
      run: |
        source $CI_PROJECT_ROOT/ci/micropython.sh
        micropython_version
        ci_cmake_configure ${{ matrix.board }} ${{ matrix.variant }}

    - name: "MicroPython: Build"
      shell: bash
      run: |
        source $CI_PROJECT_ROOT/ci/micropython.sh
        ci_cmake_build ${{ matrix.board }} ${{ matrix.variant }}

    - name: "Artifacts: Upload .uf2"
      uses: actions/upload-artifact@v7
      with:
        name: ${{ env.CI_RELEASE_FILENAME }}.uf2
        path: ${{ env.CI_BUILD_ROOT }}/${{ env.CI_RELEASE_FILENAME }}.uf2
        
    - name: "Artifacts: Upload .uf2 (With Filesystem)"
      uses: actions/upload-artifact@v7
      with:
        name: ${{ env.CI_RELEASE_FILENAME }}-with-libs-and-examples.uf2
        path: ${{ env.CI_BUILD_ROOT }}/${{ env.CI_RELEASE_FILENAME }}-with-libs-and-examples.uf2

    - name: "Release: Upload .uf2"
      if: github.event_name == 'release'
      uses: softprops/action-gh-release@v3
      with:
        files: ${{ env.CI_BUILD_ROOT }}/${{ env.CI_RELEASE_FILENAME }}.uf2

    - name: "Release: Upload .uf2 (With Filesystem)"
      if: github.event_name == 'release'
      uses: softprops/action-gh-release@v3
      with:
        files: ${{ env.CI_BUILD_ROOT }}/${{ env.CI_RELEASE_FILENAME }}-with-libs-and-examples.uf2